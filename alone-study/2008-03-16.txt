< 1과목 >

1. 메일서버를 통해서 수시로 주고받은 메일들이 저장되는 디렉토리로
만약 해당 디렉토리의 할당된 디스크 용량을 모두 사용 할 경우 시스템이 
갑자기 느려지게 되는 문제가 발생할 수 있다.
- /var

2. 쉘을 변경
- chsh

3. 디스크 추가 시 사용해야하는 명령어
- fdisk(장치확인) - mkfs(포맷) - mount(연결)

4. Filesystem Type Size Used Avail Use% Mounted on
/dev/sda1 ext3 96M 12M 84M 12.5% /boot
/dev/sda2 ext3 10G 5.4G 4.6G 54%
- df -Th

*) 추가 
- -T : 파일시스템 종류를 보여준다.
- -h : 읽기 쉬운 용량 표시 단위로 표시한다.

5. 현재 디렉토리를 포함하여 하위에 있는 디렉토리의 파일 용량까지 한번에 보여주는 명령어
- du

6. suspend
- 메모리에 올라가 있지만 정지된 상태

7. 인터넷 슈퍼 데몬이라고 불리는 것
- xinetd

8. Foreground로 동작 중인 프로세스를 suspend 할 때 쓰는 단축키
- Ctrl + z

9. 매주 새벽 4시 정각에 백업 스크립트인 데이터를 백업을 하려고 한다 
다음 crontab의 설정
- 00 4**0/이용하고자 하는 백업스크립트 이름

10. top 명령에서 CPU 사용률에 따라 정렬할 때 사용하는 명령어
- P

11. PPID를 확인하려고 할때 쓰는 명령어
- ps -f

12. 프로세스의 상태를 모니터링하는 명령어
- ps
- pstree
- top

13. kill 명령어는 프로세스 이름으로 종료할 수 없다. 
무조건 프로세스 식별자 (PID) 를 적어야한다.

14. 쉘의 종류
- csh
- bash
- ksh

15. 쉘 변경 명령어
- exec csh
- chsh -s /bin/csh
- csh

*) 추가
- echo $SHELL : 현재 사용중인 쉘을 보여준다.

16. 디렉토리 관련 정보를 담고 있는 환경 변수
- HOME : 사용자 홈 디렉토리
- PATH : 실행 파일의 경로
- PWD : 현재 작업중인 디렉토리 

*) 추가
- USER : 현재 로그인한 사용자의 환경변수

17. 쉘 명령에서 내용이 화면에 출력
- cat < /etc/inittab

18. bash에서 주석문의 시작
- #

19. 화면에디터 
- vi
- pico
- emacs

20. vi 명령모드에서 커서 이동명령
- l
- h
- k

21. vi에서 편집 작업 중 유닉스 명령을 실행시키는 방법
- ! <유닉스명령>

22. 비모드형 편집기로 텍스트 편집 기능은 물론 컴파일, 강력한 도움말등을 지원할 수 있는 편집기
- emacs 

23. RPM에서 제공하는 기능
- 자동설치
- 업그레이드
- 시스템 검증

24. RPM
- 추가 및 기존 파일의 업그레이드 가능
- 컴파일 할 필요 없이 자동적으로 설치
- 의존성 무시 설치 가능
- 패키지 상태와 파일 크기가 다른가를 체크가능

25. RPM 명령 중 설치된 패키지를 제거하는 명령
- rpm -e 패키지 이름

26. dump 명령어는 파일 시스템 자체를 직접 읽어내는 방법을 사용하여 파일들이
아닌 파일시스템 전체를 백업한다.
파티션 단위로 백업할 때 유용하게 사용

27. make 명령을 이용할 때 설치 과정에 꼭 필요한것
- Makefile

28. 부팅 시 랜카드, 사운드카드 등의 드라이버(모듈)가 자동으로 
커널에 적재되도록 설정해주는 설정 파일
- /etc/modprobe.conf

29. 각종 하드웨어의 모듈이 저장되어 있는 디렉토리
- /lib/modules/<커널버전>/kernel/drivers

30. LPRng를 사용하여 프린터를 설치 하려고 할 때 설정해야하는 파일
- /etc/printcap

31. MP3 재생 프로그램
- xmms

< 2과목 >

1. 스크립트의 구동 순서
- xinitrc → .Xresources → .Xmodmap → Xclients

2. 네트워크로 연결된 상태나 활성화된 소켓 등을 확인할 수 있는 명령어
- netstat

3. 게이트웨이를 확인 할 때 사용할 수 있는 명령어
- netstat

*) 윈도우의 ipconfig는 게이트웨이까지 확인 가능, 
리눅스의 ifconfig에서 보이는 것은 브로드캐스트 뿐
게이트웨이를 확인하기 위해서는 netstat -r옵션 사용,
route 명령어로 게이트웨이 확인

4. domain 53/top을 확인할 수 있는 리눅스 시스템의 설정파일
- /etc/services

5. SSH 포트 번호 
- 22

6. 대화제어자로서, 동기화 역할 수행, 사용자의 연결 유효 여부확인하는 계층
- 세션계층

7. 기관
- or : 비영리기관
- go : 정부기관
- ac : 교육기관
- co : 영리기관

8. 호스트들이 파일을 공유할 수 있도록 해주는 프로토콜
- NFS

9. FTP를 이용하여 파일을 송수신할 때 전송상태 확인하는 명령어
- hash

10. 아파치2의 더 나은 기능
- 멀티쓰레드
- autoconf, libtool을 컴파일이 사용가능
- API로 구현
- IPv6지원
- Listen 지시어만 사용

11. loopback interface에서 사용하는 IP 주소
- 127.0.0.1

12. 모뎀과 전화선을 이용할 경우 가장 관계가 깊은 프로토콜 
- PPP

13. FTP 클라이언트 프로그램
- ncftp
- lftp
- gftp

14. 임베디드 시스템
- 소규모 모듈 단위로 설계
- 리얼타임 운영을 지원

15. 가상화 소프트웨어
- Xen
