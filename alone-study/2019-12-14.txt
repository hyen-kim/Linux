1과목 리눅스 운영 및 관리 15/48
2과목 리눅스 활용 20/32 

< 1과목 >

1. lin.txt 파일의 소유권은 ihduser, 그룹 소유권을 admin으로 변경하는 명령어
- chown ihduser.admin lin.txt

2. 디렉터리에 부여되는 x권한에 대한 설명
- 해당 디렉터리의 내부로 접근할 수 있다.

3. #monut-o loop CentOS-6.10-i386.bin-DVD.iso
    /media

4. 마운트된 .home2를 해제시키는 과정
#umount /home2

5. 파일이나 디렉터리의 소유권을 확인하는 명령
- ls

*) 추가
- chmod : 접근권한 변경
- chown : 소유자변경
- chgrp : 그룹변경
 
6. 파일의 허가권 값
$umask -S 
u = rwx, g=rwx, o=rx

<해설>
r=4, w=2, x=1
파일 기본권한은 666 디렉토리 기본권한은 777
u=rwx, g=rwx, o=rx -> 775
-2이므로 666-2 -> rw  rw  r

7. 일반 사용자 환경에서 셸 변경시 사용 : chsh
Shell changed 가 나오면 무조건 chsh

8. 1987년 버클리 대학의 빌 조이가 개발한 것으로 히스토리 기능,
alias 기능, 작업 제어 등의 유용한 기능을 포함
- csh

9. 사용 가능한 셸의 목록을 확인하는 명령
- cat /etc/shells

10. # ps aux 명령 실행 시 확인할 수 있는 것
- 총 CPU 사용 시간
- 프로세스 식별번호
- 현재 프로세스 상태코드

11. Ctrl + Z를 입력했을 때 발생하는 시그널 이름
- SIGTSTP

*) 추가
- SIGINT : ctrl + c
- SIGTERM : 프로세스 종료
- SIGSTOP : 프로세스 중지

12. top 실행상태에서 프로세스와 CPU 항목을 on/off 하는 명령
- t

13. 멀티태스킹
- 백그라운드 프로세스의 작업 상태는 jobs 명령으로 확인
- Foreground 와 Background 프로세스 간 전환이 가능
- 작업 중인 프로세스를 대기시키는 키 조합은 ctrl + z

14. 프로세스
- init 프로세스는 모든 프로세스의 부모 프로세스
- exec는 원래 프로세스의 메모리에 새로운 프로세스의 코드를 덮어씌운다.
- fork는 새로운 프로세스를 위해 메모리 할당 받아 복사본 형태로 실행

15. 저장된 crontab 설정 파일을 삭제하기 전에 사용자에게 확인한다.
- crontab -ir

*) 추가
- i: information
- r: remove

16. 매주 월요일과 목요일 오전 10시에 점검 스크립트인 /etc/check.sh가 실행되도록 설정
- 0 10 * * 1,4 /etc/check.sh

17. 유닉스 계열 시스템의 대표적인 편집기는 ( vi )
( vi )에 추가 기능을 대폭 강화하여 만든 편집기는 ( vim )
이를 GUI 기반으로 개발한 편집기는 ( gVim)

18. vi 편집기에서 ex 명령 모드로 진입하기 위해서는 명령 모드에서 :
를 입력하면 화면 아래쪽에 : 라는 프롬프트가 나타나고 이를 ex 명령 모드라고한다.

19. 소스 설치 과정에서 디렉터리를 지정하는 단계
- --prefix 

20. 소스 설치 기법으로 cmake를 이용하는 프로그램
- MySQL

21. 데비안 계열 리눅스에서 사용하는 패키지 관리 기법
- apt
- apt -get
- dpkg

22. 동일한 소스 파일을 묶어서 압축했을 때 파일의 크기가 가장 작게 생성되는 파일
- php-7.3.11.tar.xz

-> .xz는 가장 최근에 배포된 것으로 압축률이 가장 좋다.


23. telnet이라는 문자열이 있는 패키지를 찾아보는 과정
# yum search telnet

24. lpr 명령어를 이용하여 프린터 출력을 하려고할 때 인쇄할 매수를 지정하는 옵션
- -#

25. 애플이 개발한 오픈 소스 프린팅 시스템
HTTP 기반의 프로토콜을 사용
BSD 와 System V 명령어 모두 사용 가능
- CUPS

26. 음악 파일을 추출할 때 사용하는 명령
- cdparanoia

< 2과목 >

1. 디스플레이 매니저
- 디스플레이 매니저는 사용자 이름과 암호가 유효한 값일 때 세션을 시작

2. 프로젝트는 2004년에 시작되어, 기존에 사용되던 프로젝트의 라이선스 정책 변화로 인하여
관련 개발자들 대부분이 이 프로젝트에 합류하였고,
현재 리눅스 비롯하여 유닉스의 대부분이 이 프로젝트에서 개발된 x.org을 사용

3. X 윈도우는 서로간의 통신을 위해 X Protocol을 사용한다.
X 윈도우는 기본 구조가 네트워크 기반이기 때문에 
로컬의 사용자가 키보드나 마우스로 입력하면 TCP 포트 6000번을 
통해 전달한다.

4. 상위 계층으로부터 전달받은 데이터에 인접한 장치의 물리 주소인 MAC 주소, 
데이터의 정확한 송수신을 위한 규정이나 통신 회선 접속의 확인 등과 같은 
전송 제어의 역할을 수행한다
- 데이터링크 계층

5. OSI 7계층의 프로토콜 데이터 단위를 하위 계층부터 순서대로
- frame -> packet -> segment

6. T568B 배열의 순서
- 흰색/주황색, 주황색, 흰색/녹색, 파란색, 흰색/파란색, 녹색, 흰색/갈색, 갈색

7. 메일 서버에 도착한 메일을 사용자 컴퓨터에서 확일할 때 사용하는 프로토콜
- POP3, IMAP

8. NETWORKING=yes
HOSTNAME=www
- etc/sysconfig/network

9. 로컬 네트워크에 있는 다른 시스템에 장착된 이더넷 카드의 MAC 주소를 확인할 때 사용하는 명령
- arp

10. 컴퓨터 기종에 상관없이 정보 교환이 가능
- TCP/IP

11. 이탈리아 한 회사에서 하드웨어에 익숙지 않은 학생들을 위해 오픈 소스 기반으로
한 하나의 단일 보드마이크로컨트롤러이다. 완성된 보드와 관련 개발 도구 및 환경을 제공
- Arduino


